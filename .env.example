# Copy to .env for local development. All values below are the defaults.

# Stage: local | dev | prod
STAGE=local

# Local HTTP server
PORT=3333
HOST=0.0.0.0
LOG_LEVEL=info

# Web app URL (used for CORS and, later, e-mail links)
APP_URL=http://localhost:3000
